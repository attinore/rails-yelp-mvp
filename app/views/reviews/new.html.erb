<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :rating, collection: 0..5, as: :radio_buttons, wrapper: :vertical_collection_inline %>
  <%= f.input :content %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
